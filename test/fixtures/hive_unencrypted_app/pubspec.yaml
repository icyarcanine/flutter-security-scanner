name: hive_unencrypted_app
description: Fixture for insecure-storage rule (Hive without encryptionCipher).
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
