name: realtime_filter_ok_app
description: Negative fixture — Realtime subscription scoped by filter.
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.0.0
  supabase_flutter: ^2.0.0
