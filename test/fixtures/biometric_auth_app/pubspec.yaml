name: biometric_auth_app
description: Fixture for the biometric-auth rule (misconfigured local_auth).
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  local_auth: ^2.1.0
