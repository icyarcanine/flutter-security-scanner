name: edge_function_secrets_app
description: Positive fixture — edge function with verify_jwt=false, CORS wildcard, service-role leak.
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.0.0
  supabase_flutter: ^2.0.0
